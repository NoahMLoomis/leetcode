function operation1(word1: string, word2: string): boolean {
}


function closeStrings(word1: string, word2: string): boolean {
  return operation1(word1, word2)
};

console.log(closeStrings('abc', 'bca'))
